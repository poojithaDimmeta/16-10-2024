#include<stdio.h>
#include<string.h>
#include<conio.h>
void main()
{
 char a[20]="MALAYALAM";
 int l=strlen(a)-1;
 for(int i=0,j=l;i<=j;i++,j--)
 {

     if(a[i]!=a[j])
     {
         printf("it is not palindrome");
         return;
     }
     }
printf("it is palindrome");
}

